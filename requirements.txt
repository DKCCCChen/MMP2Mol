# Python runtime used by the corresponding Conda environment: Python 3.7.13
# Install the requested pip version separately if exact reproduction is needed:
#   python -m pip install pip==22.1.2
#
# PyPI does not publish the historical mmpdb 3.1 release used here. The exact
# v3.1 tag is therefore installed from the official RDKit/mmpdb repository.
# This line requires Git to be available on PATH.

numpy==1.21.5
pandas==1.3.5
scipy==1.7.3
scikit-learn==1.0.2
joblib==1.2.0
xgboost==1.6.2
tensorflow==2.11.0
rdkit==2022.3.5
mmpdb @ git+https://github.com/rdkit/mmpdb.git@v3.1
matplotlib==3.5.3
seaborn==0.11.1
tqdm==4.64.1
